[general]
pythonfile = ~/.offlineimap.py
accounts = Work

[Account Work]
localrepository = Local
remoterepository = Remote

[Repository Local]
type = Maildir
localfolders = ~/mail

[Repository Remote]
type = IMAP
remotehost = box.projectmakeit.com
remoteuser = blixa@projectmakeit.com
remotepasseval = mailpasswd("mailbox")
sslcacertfile = OS-DEFAULT
